# 財智管家 FinPilot — 專案現況（2026-07-12）

給 Claude Code 接手開發用。這份文件是「目前實際狀態」的持續更新紀錄，不是一次性交接文件——每次工作階段結束後都應該更新這份檔案，而不是另外新增一份新文件。舊的 `docs/專案交接文件.md` 是最早期（第一、二階段）的規劃文件，內容已經大部分被本文件取代，保留僅供歷史參考。

## 專案基本資訊

- 個人使用的記帳+財富管理系統，網站+手機PWA，資料跨裝置同步
- 前端：純原生 HTML/CSS/JS（無框架），單一 SPA，`index.html` + `app.js` + `styles.css`
- 資料庫：Supabase（Postgres + Auth + RLS），schema 全部建在 `expense_app` 底下（不用 `public`，避免跟同專案其他App混在一起）
- 部署：Vercel（GitHub push 自動部署）
- **工作流程慣例：每次改完功能，測試過後要自動 commit + push，不用額外確認**
- Supabase 的 SQL migration 檔案全部存放在 `docs/*.sql`，依執行順序命名，每個都是「一次性執行過就不用再跑」，已執行過的都會在檔案開頭註記狀態

## 一、今天完成的工作（2026-07-12）

### 完成股票即時股價功能的最後一哩路
- 使用者確認已執行 `docs/supabase-股票持股欄位.sql`、已透過 Supabase Dashboard「Via Editor」部署 `get-stock-price` Edge Function
- 實測發現原本美股資料源 Stooq 已失效：`/q/l/` 即時報價端點回傳 404（已停用/搬移），備用的 `/q/d/l/` 端點則被 anti-bot JS挑戰保護擋住（伺服器端 fetch 無法通過）。改用 Yahoo Finance 的 `query1.finance.yahoo.com/v8/finance/chart/{symbol}` 端點，實測可正常取得美股即時價格與幣別，已重新部署
- 用瀏覽器工具+使用者本人正式帳號完整實測：台股 2330（$2,415 TWD）、美股 AAPL（$315.32 USD）皆能正確抓價；抓到的價格 × 股數正確帶入「目前價值」欄位；送出後資產列表的損益金額/百分比計算正確（測試案例：100股台積電損益 +$191,500/+383.0%，10股AAPL損益 +US$1,653.20/+110.2%）；無效股票代碼會顯示合理錯誤訊息且表單不會卡死。測試用資產已從正式帳號刪除

### 記帳系統視覺主題統一成深色金色
- 使用者反應記帳系統(淺色綠)跟財富管家(深色金)切換時明暗反差太強烈，改成記帳系統也套用深色金色主題（`--primary`系列從綠色改金色`#C9A25D`），詳見下方技術決策#3

### 家庭共同基金帳本：家人帳號建立+歷史資料匯入
- 家人（chukevin57@gmail.com）已建立帳號並登入，在自己帳號底下新增「家庭共同基金」帳本，把先前轉換好的 `家庭共同基金匯入檔.json` 匯入（2260筆交易，0筆失敗）
- 匯入後家人反映「收入顯示$0」，查證後確認不是bug：全部2260筆交易裡只有15筆是收入類型（總額$1,186,550），日期分散在2022~2026年3月，剛好都不在匯入當下的月份，總覽預設顯示「本月」所以顯示$0是正常的，切換「按年」或用列表篩選就能看到

### 帳戶/財富管家bug修正
- 記帳系統的帳戶管理列表，已停用的帳戶原本沒有排序、混在啟用中的帳戶之間，改成停用的排到最後
- 財富管家的帳戶明細/淨資產計算，原本只檢查帳戶自己的`is_archived`，沒檢查帳戶所屬帳本是否已封存，導致封存整個帳本後底下帳戶還會出現在財富管家，已修正為一併排除已封存帳本底下的帳戶
- 財富管家帳戶明細清單，餘額為$0的帳戶（例如用不到的分享帳本預設帳戶）改成排序到清單最後面，不隱藏/不刪除資料

### 記帳系統UX改善
- 新增交易的「商家」欄位，原本是要滑很久的下拉選單，改成文字輸入+自動完成清單（datalist），打字就能快速篩選，選定後仍對應到商家的UUID存進資料庫

### 財富管家功能擴充
- **淨資產含外幣換算**：原本淨資產/資產配置圓餅圖/歷史趨勢圖只計入台幣帳戶跟資產，外幣（例如美股、人民幣帳戶）完全被排除。改用免費即時匯率API（`open.er-api.com`，可直接前端呼叫、不會被CORS擋，這點跟股價API不同）把外幣折算成台幣一併計入。帳戶明細清單本身仍顯示各帳戶原始幣別金額，方便核對。趨勢圖受限於免費API只有「現在」的即時匯率、沒有歷史每日匯率，過去時間點的外幣換算一律套用「今天的匯率」回推，不是精確歷史值（已跟使用者說明這個限制）
- **外幣部位總覽卡片**：總覽新增一張卡片，跨帳戶+投資資產依幣別加總、顯示原始幣別金額（不折算台幣），方便掌握手上實際持有多少美金/人民幣等外幣部位
- **一鍵更新所有股價**：資產明細加「更新所有股價」按鈕，批次抓取所有有代碼+股數的投資資產最新股價，寫入今天的快照（同一天重複按只會更新、不會產生重複快照），完成後顯示成功/失敗檔數。原因：股價原本只有新增/編輯資產當下手動抓一次會更新，之後打開總覽畫面不會自動重新查詢，淨資產數字會隨時間跟實際股價脫節
- **資產列表顯示目前股價**：股票類資產在分類旁邊多顯示「股價 $xxx」，用市值÷股數反推（資料庫沒有另外存股價欄位，市值和股價互為對稱運算，每次都是即時反推，不是只有第一次算），方便核對「更新所有股價」抓到的價格對不對
- **股票損益顏色改成台股慣例（紅漲綠跌）**：獲利用紅色`#E5484D`（原本試過`--w-rust`磚紅色但使用者覺得不夠鮮明，改成更飽和的紅）、虧損用綠色`--w-sage`，新增專用的`.w-item-sub.w-pnl-positive`/`.w-item-sub.w-pnl-negative`樣式（雙class選擇器拉高CSS優先度，避免被`.w-item-sub`自己的預設灰色規則蓋掉——踩過一次CSS規則順序的坑），不影響其他地方（例如負債金額）原本西式紅黑配色的`.w-negative`
- 手機版底部導覽列文字放大到跟記帳App一致（`.w-nav-btn`原本漏了手機寬度的字體放大規則）
- 平板寬度（768-1400px，走桌面版側邊欄排版但螢幕比筆電大）文字放大：**先試過`zoom:2`整體放大，結果版面可用空間被砍半，多欄排版被擠爆、文字疊成一直行，緊急撤掉**，改成只針對標題/列表/導覽等文字個別加大字體、完全不動格線欄寬，才成功

### PWA manifest 修正
- `manifest.json` 的 `"orientation": "portrait"` 會讓已安裝到主畫面的PWA在平板橫向使用時，畫面被壓縮成一小格塞在角落、其餘全黑。移除這個鎖定，順便把`background_color`/`theme_color`從舊的淺綠主題改成新的深色主題色

### 帳本分享（唯讀）功能——全新完整功能
- 需求：家庭共同基金帳本的擁有者(chukevin57)可以用email邀請其他已有帳號的人，讓對方能「看」這本帳本但完全不能寫入
- 資料庫新增 `expense_app.ledger_shares` 表（ledger_id/owner_user_id/viewer_email），權限比對直接用 `auth.jwt()->>'email'` 比對，不查`auth.users`表、不做邀請/接受流程——擁有者輸入email存起來，對方用那個email登入立刻生效
- `ledgers`/`accounts`/`transactions`/`budgets`/`recurring_rules`/`categories`/`merchants` 共7張表的RLS政策，從「讀寫都只認自己」拆成「讀取：自己或被分享的人都可以」「寫入：只有自己」，寫入權限完全沒開放給被分享者，就算前端畫面漏藏按鈕，資料庫這層還是會擋下寫入
- 前端：帳本管理畫面可以邀請/移除分享對象；帳本切換下拉選單裡被分享的帳本會標示「(分享・唯讀)」；切到不是自己擁有的帳本時，畫面套用`body.ledger-readonly`，把新增/編輯/刪除等所有寫入動作的UI都隱藏，並顯示「唯讀」標籤
- 修正兩個因分類/商家改成可跨帳號讀取而冒出來的連帶問題：(a) `seedDefaultsIfEmpty()`原本用「有沒有看得到任何分類」判斷要不要幫新帳號建預設分類，分享關係開通後會被擁有者的分類混淆而誤判，改成只計算「自己名下」的分類/商家數量；(b) 帳本自動建立預設帳戶的邏輯，改成只在查看自己擁有的帳本時才執行，避免被分享者不小心把預設帳戶寫進別人的帳本
- 已由使用者實測確認正常運作

### 出差/旅遊行程功能——全新獨立模組（第一版，文字記錄不含拍照）
- 評估過整合到這個App vs 另開「隨身點子App」（Next.js+TypeScript+Supabase技術棧，跟這裡的純HTML/CSS/JS不同）之後，決定先在這裡做第一版，因為設計上可以做到完全獨立、之後要刪除或搬遷都很乾淨（見下方技術決策）
- 資料庫新增6張表：`trips`（行程主檔）、`trip_expenses`（花費，會加總成總花費）、`trip_attractions`（景點）、`trip_transportation`（交通）、`trip_lodging`（食宿）、`trip_notes`（記事），RLS都是簡單的`auth.uid()=user_id`，不做分享
- 記帳模式底下新增「行程」分頁：行程列表 → 點進單一行程 → 花費/景點/交通/食宿/記事五個子分頁，各自可新增/編輯/刪除
- 私人行程可以「結束行程，記一筆到記帳」，把總花費（依行程主幣別加總）用一筆交易存進當前帳本的「旅遊記錄」分類（分類不存在會自動建立），出差行程不會動到記帳系統；用交易備註嵌入行程ID當標記，重複結算時會跳出確認提示避免不小心重複記兩次
- 交通的出發/抵達時間，原本用`<input type="datetime-local">`，實測發現這種輸入框的12/24小時制、上午下午顯示，是瀏覽器依裝置系統locale決定、網頁端無法強制指定（使用者的平板顯示成12小時制+上午下午選單）。改成日期+文字輸入（HH:MM，pattern驗證）分開兩欄，才能保證一定是24小時制
- 子分頁列加「總表」選項，一次顯示全部五類清單（隱藏各自的新增表單只看資料），並提供「匯出PDF」（用瀏覽器原生列印機制+`@media print`，不需要額外套件）「匯出Word」（HTML包裝成.doc檔下載，Word可直接開啟，不是真正docx格式但文字/表格內容完整）
- 已由使用者實測確認功能正常，並根據回饋做了多輪視覺調整（記事字體、總花費字體對齊行程名稱、行程列表文字放大等）

### 部署流程的經驗教訓
- 這次session發現Vercel的GitHub webhook會偶發漏接（commit已經push到GitHub、但Vercel完全沒有觸發部署，連失敗紀錄都沒有，不是建置失敗是根本沒開始跑），確認方式是直接`curl`打正式站比對是否有新程式碼的特徵字串，比看Vercel Dashboard的Deployments列表更快。遇到這種情況用`git commit --allow-empty`補推一次空白commit就能重新觸發，每次改完程式碼後都應該主動curl驗證部署成功，不要只看「push成功」就當作已經上線

## 一（續）、更早完成的工作（2026-07-11，涵蓋整個工作階段的所有 commit）

### 歷史資料匯入
- 修正 JSON 匯入迴圈遇到單筆錯誤會整批中斷的問題，改成逐筆 try/catch、失敗就跳過並記錄筆數
- 修正 `sbGetAllTransactions()` 沒有分頁，交易數超過 Supabase 預設 1000 筆上限會被截斷的問題，改成 `.range()` 迴圈分批抓取
- 把使用者的 AndroMoney 歷史記帳資料（個人+家庭共同基金，共兩份 CSV）轉換匯入，過程中修正了分類簡化、收入分類重新歸類、重複帳戶/重複交易清理、帳戶餘額校正等多個資料清理問題

### 記帳系統 UI/UX 全面調整
- 交易紀錄新增編輯功能、編輯畫面加刪除按鈕
- 交易列表：文字階層調整、類型切換+加總、金額對齊修正、日期改升冪排序
- 總覽：加年度檢視、支出/收入切換、「本月」快速跳轉按鈕
- 響應式排版大改：桌面/平板改用左側邊欄導覽、總覽多欄排版、新增交易表單雙欄化（計算機+欄位分開），修正切換分頁時舊畫面未正確隱藏的嚴重bug
- 整體視覺質感提升（卡片陰影、按鈕漸層、SVG圖示取代文字符號）
- 修正多個小bug：手機版按鈕懸浮陰影、金額輸入框只能整數（改成 `step="0.01"`）、切走新增畫面殘留編輯狀態

### 新功能
- **每月支出預算**：`expense_app.budgets` 表，單一總預算（非分類別），總覽按月檢視顯示已用/剩餘，超支變紅
- **定期定額交易**：`expense_app.recurring_rules` 表 + `transactions.recurring_rule_id` 欄位，預先產生未來12個月的真實交易列（不是預覽層），未來日期用淺灰色顯示，到期自動變色；編輯可選擇「套用到未來月份」批次更新
- **外幣帳戶＋換匯轉帳**：帳戶可選幣別（TWD/USD/EUR/CNY/JPY），`transactions.transfer_to_amount` 欄位支援轉出/轉入金額不同（換匯情境），匯率輸入採台灣銀行慣用「1外幣=多少台幣」報價方式
- **財富管家（個人財產管理）**：全新的深色金色主題子系統，跟記帳系統用同一個 App、透過導覽列互相切換
  - `expense_app.assets` + `asset_snapshots`（資產，快照式估值追蹤）
  - `expense_app.liabilities` + `liability_snapshots`（負債，快照式餘額追蹤）
  - 總覽畫面：淨資產/總資產/總負債/負債比、資產配置圓餅圖、跨帳本即時帳戶明細
  - 歷史趨勢圖：依快照日期重建過去的淨資產（SVG折線圖）
  - 投資類資產支援股票持股細項（市場/代碼/股數/成本），自動算損益；新增 Supabase Edge Function `get-stock-price` 代理抓取台股/美股即時股價（原因見下方「技術決策」）

## 二、目前卡住的地方 / 還沒測試的部分

**目前沒有阻塞點。** 這次工作階段做的所有功能（股票即時股價收尾、帳本分享唯讀、財富管家外幣換算/股價顯示/一鍵更新、出差旅遊行程模組、PWA平板顯示修正）都已經由使用者本人或家人實測確認正常運作，沒有已知問題。

## 三、下一步具體動作（依序）

1. 出差/旅遊行程功能目前是第一版（文字記錄），使用者提到之後如果需要「拍照」功能要另外評估——目前這個App完全沒碰過檔案上傳/儲存的能力，要接 Supabase Storage，工程量比純文字記錄大不少，先觀察第一版夠不夠用再決定要不要做
2. 陸股股價資料源目前明確不支援，先讓使用者手動輸入，之後如果有穩定資料源再評估要不要加
3. 待財富管家/行程功能整體穩定後，使用者提到還想再討論的方向（非阻塞、之後再談）：
   - 家庭共同基金帳本的分享對象目前只有chukevin57邀請，之後如果有更多人要加入，流程已經跑通（帳本管理畫面輸入email邀請即可），不需要額外開發

## 四、跟原本規劃不同的技術決策

1. **記帳系統的外幣不自動換算成台幣總額**——總覽的支出/收入/結餘、分類圖表只計入台幣帳戶的交易，外幣交易不併入避免不同幣別金額直接相加造成誤導。這是跟使用者確認過的決策，不是遺漏。
2. **財富管家的現金/銀行類資產，不走 `assets` 表手動輸入，而是直接即時抓取記帳系統裡所有帳本的帳戶餘額**（跨帳本聚合，見 `app.js` 的 `sbGetAllTransactionsAllLedgers()` 跟 `computeWealthAccountBalance()`）。這跟最初 SQL 設計文件裡「assets.category 可以是 cash」的構想不同——現金餘額本來就已經在記帳系統即時準確地追蹤，沒必要在財富管家裡重複手動輸入一次，兩邊容易對不上。`assets` 表現在只用來放 investment/real_estate/precious_metal/crypto/insurance/other 這幾類需要「手動估值快照」的資產。
3. ~~財富管家跟記帳系統是兩套完全獨立的視覺主題（深色金色 vs 淺色綠色）~~ **【2026-07-12更新，此決策已推翻】**：原本刻意做成兩套獨立主題，但實際使用後使用者反應兩邊切換時明暗反差太強烈（淺色↔深色）。改成記帳系統也套用跟財富管家一致的深色金色主題（`styles.css` 的 `:root` 變數：`--bg`/`--card`/`--text-*`/`--border` 改深色系，`--primary`/`--primary-dark`/`--primary-light` 從綠色改成跟財富管家一樣的金色 `#C9A25D`）。原因：深綠色 `--primary` 在深色背景上對比不足、文字難辨識，且使用者認為金色在深色背景上效果本來就比較好，兩邊統一也比較舒服。兩邊各自導覽列有一個入口可以切換到對方，這點不變。
4. **定期定額交易採用「預先產生未來12個月的真實交易列」**，不是畫面上疊加一層預覽、不寫進資料庫。原因：使用者要能直接在交易列表看到未來排定的收支、也要能直接編輯/刪除單筆，用真實資料列最直接。用日期比對（未來日期淺灰顯示、日期一到自動變正常色）取代额外的狀態欄位，不需要背景排程。
5. **每月預算先只做單一總預算，不分類別**——是跟使用者確認過的範圍決策，之後如果要分類別預算，`budgets` 表可以再擴充。
6. **股票即時股價需要新增 Supabase Edge Function 當代理**，這是超出原本「純前端PWA＋Supabase資料庫」架構的新基礎設施。原因是**實測**（不是理論猜測）瀏覽器直接呼叫台股 TWSE 或美股資料源，都會被 CORS 擋掉（`Failed to fetch`），只能透過伺服器端（Edge Function 對伺服器呼叫沒有 CORS 限制）代理才能真正抓到股價。陸股目前沒有找到穩定資料源，先讓使用者手動輸入。
   - **美股資料源後來從 Stooq 改成 Yahoo Finance（2026-07-12）**：原本選用的 Stooq 免費CSV端點，實測時發現已失效——`/q/l/` 即時報價端點回傳純 404（伺服器端已下架/搬移，不是CORS或程式問題），備用的 `/q/d/l/` 下載端點則被新增的 anti-bot JS挑戰頁保護，Deno Edge Function 的純伺服器端 fetch 無法通過（沒有瀏覽器JS執行環境可以解題）。改用 Yahoo Finance 的 `query1.finance.yahoo.com/v8/finance/chart/{symbol}` 端點取代，實測穩定可用。
7. **JSON匯入的錯誤處理原則從「一筆錯誤就整批中斷」改成「逐筆記錄失敗、繼續處理其餘資料」**——這是在匯入使用者真實歷史資料時發現的嚴重bug（原本的寫法會導致匯入 2321 筆資料時，第一筆網路瞬斷就整批放棄，只成功不到5%），修正後所有匯入/批次寫入迴圈都遵循「單筆失敗不影響整批」的原則。
8. **帳本分享（唯讀）用email直接比對，不做邀請/接受流程、不查`auth.users`表**——考慮過標準的「發邀請→對方接受」流程，但這是小型家庭用途的App、不是公開SaaS，簡化成「擁有者輸入email存進`ledger_shares`表，RLS政策直接拿`auth.jwt()->>'email'`比對」，對方用那個email登入立刻生效，省掉一組邀請狀態機跟UI，寫入權限完全不受影響（分享只開放SELECT，INSERT/UPDATE/DELETE政策維持只認`user_id`）。
9. **出差/旅遊行程功能刻意設計成跟記帳系統/財富管家完全獨立的一組新表**（`trips`/`trip_expenses`/`trip_attractions`/`trip_transportation`/`trip_lodging`/`trip_notes`），沒有任何欄位外鍵關聯到既有表，唯一的接點是「私人行程結束後可以選擇手動記一筆總花費到記帳系統」，但那產生的就是一筆普通交易，資料庫層面看不出來源。這是因為使用者一開始就明確表示這個功能有可能後來決定不整合進這個App、想搬去另一個「隨身點子App」（Next.js+TypeScript技術棧），設計成低耦合是為了確保「刪除這個功能」或「把設計搬到別的技術棧重新實作」都很乾淨，不會動到既有的記帳/財富管家資料。
10. **行程總表匯出PDF/Word不使用任何前端函式庫**——PDF用瀏覽器原生`window.print()`+`@media print`CSS（把頁面其他部分隱藏、只印一個動態組出來的`#trip-print-area`），Word用HTML字串包裝成`Blob`、副檔名`.doc`、MIME type `application/msword`下載（Word可以直接開啟HTML內容，不是真正的docx二進位格式，但文字/表格排版對這個用途已經足夠）。這是刻意的技術選擇，避免為了一個匯出功能引入jsPDF之類的重量級套件，符合這個專案「純原生HTML/CSS/JS無框架」的既有架構。
11. **股票即時股價顯示改用「反推」而非額外存欄位**——資產列表顯示的「股價」不是資料庫另外存的欄位，而是每次用「快照存的市值 ÷ 股數」現場算出來，因為市值本來就是`股數×股價`存進去的，兩者互為對稱運算，不需要多一個欄位、也不會有兩個數字對不上的風險。
12. **股票損益顏色改成台股慣例（紅漲綠跌），跟財富管家原本的負數配色分開處理**——原本`.w-negative`（用於負債等一般負數金額）維持西式配色（紅色=負），但股票損益的語意是「正的=獲利」不是單純的正負號，使用者要求正的用紅、負的用綠，所以新增專屬的`.w-pnl-positive`/`.w-pnl-negative`樣式，不去動`.w-negative`本身，避免影響負債等其他畫面。
13. **Vercel的GitHub webhook偶發性漏接**——實測發現至少兩次push之後Vercel完全沒有觸發新的部署（不是建置失敗，是連嘗試都沒有），透過`curl`直接比對正式站內容才發現。目前的應對方式是每次改完程式碼、push完之後都主動用`curl`驗證正式站是否真的更新，發現沒更新就用`git commit --allow-empty`補推一次觸發重新部署，不能只看「git push顯示成功」就認定已經上線。
